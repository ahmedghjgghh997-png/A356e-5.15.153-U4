name: Build Kernel (Exynos) - Auto Source + boot.img

on:
  workflow_dispatch:
    inputs:
      kernel_source_url:
        description: 'رابط سورس النواة (ZIP, TAR.GZ, TAR.XZ, أو repo .git أو رابط Google Drive)'
        required: false
        default: ''
        type: string
      boot_direct_url:
        description: 'رابط مباشر لـ boot.img'
        required: false
        default: ''
        type: string

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # قراءة الروابط الافتراضية من الملفات
      - name: Read default kernel source URL
        id: read_kernel_url
        run: |
          if [ -f "kernel_source_url.txt" ]; then
            echo "default_url=$(cat kernel_source_url.txt | head -1)" >> $GITHUB_OUTPUT
          else
            echo "default_url=" >> $GITHUB_OUTPUT
          fi

      - name: Read default boot URL
        id: read_boot_url
        run: |
          if [ -f "boot_url.txt" ]; then
            echo "default_boot_url=$(cat boot_url.txt | head -1)" >> $GITHUB_OUTPUT
          else
            echo "default_boot_url=" >> $GITHUB_OUTPUT
          fi

      - name: Set effective kernel source URL
        id: set_kernel_url
        run: |
          if [ -n "${{ github.event.inputs.kernel_source_url }}" ]; then
            echo "effective_url=${{ github.event.inputs.kernel_source_url }}" >> $GITHUB_OUTPUT
          else
            echo "effective_url=${{ steps.read_kernel_url.outputs.default_url }}" >> $GITHUB_OUTPUT
          fi

      - name: Set effective boot URL
        id: set_boot_url
        run: |
          if [ -n "${{ github.event.inputs.boot_direct_url }}" ]; then
            echo "effective_boot_url=${{ github.event.inputs.boot_direct_url }}" >> $GITHUB_OUTPUT
          else
            echo "effective_boot_url=${{ steps.read_boot_url.outputs.default_boot_url }}" >> $GITHUB_OUTPUT
          fi

      # تثبيت البيئة الأساسية (clang-18 و lld-18 موجودان افتراضياً في Ubuntu 24.04)
      - name: Setup Build Environment
        run: |
          sudo apt update
          sudo apt install -y bc bison build-essential ccache curl device-tree-compiler \
            flex g++-multilib gcc-multilib git gnupg gperf imagemagick libc6-dev-i386 \
            libelf-dev liblz4-tool libncurses-dev libsdl1.2-dev libssl-dev \
            libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc \
            zip zlib1g-dev dwarves pahole libarchive-tools zstd kmod erofs-utils \
            unzip xz-utils python3-pip clang-18 lld-18
          pip install gdown

      # تحميل السورس
      - name: Download and Extract Kernel Source
        id: download_source
        if: steps.set_kernel_url.outputs.effective_url != ''
        run: |
          SOURCE_URL="${{ steps.set_kernel_url.outputs.effective_url }}"
          mkdir -p kernel-source
          if [[ "$SOURCE_URL" == *drive.google.com* ]]; then
            FILE_ID=$(echo "$SOURCE_URL" | grep -oP '(?<=/d/)[a-zA-Z0-9_-]+')
            gdown "https://drive.google.com/uc?id=${FILE_ID}" -O kernel-source.download
          else
            curl -L -o kernel-source.download "$SOURCE_URL"
          fi
          case $(file -b --mime-type kernel-source.download) in
            application/zip) unzip -q kernel-source.download -d kernel-source ;;
            application/gzip) mv kernel-source.download kernel-source.tar.gz && tar -xzf kernel-source.tar.gz -C kernel-source --strip-components=1 ;;
            application/x-xz) mv kernel-source.download kernel-source.tar.xz && tar -xJf kernel-source.tar.xz -C kernel-source --strip-components=1 ;;
            *) echo "Unsupported format"; exit 1 ;;
          esac
          echo "KERNEL_SOURCE_DIR=kernel-source" >> $GITHUB_OUTPUT

      - name: Use kernel source from repo folder 'kernel/'
        if: steps.set_kernel_url.outputs.effective_url == ''
        run: |
          if [ -d "kernel" ]; then
            mkdir -p kernel-source
            rsync -av ./kernel/ kernel-source/
          else
            exit 1
          fi

      # تحميل boot.img
      - name: Download boot.img
        id: download_boot
        if: steps.set_boot_url.outputs.effective_boot_url != ''
        run: |
          BOOT_URL="${{ steps.set_boot_url.outputs.effective_boot_url }}"
          mkdir -p kernel-source/stock_boot
          if [[ "$BOOT_URL" == *drive.google.com* ]]; then
            FILE_ID=$(echo "$BOOT_URL" | grep -oP '(?<=/d/)[a-zA-Z0-9_-]+')
            gdown "https://drive.google.com/uc?id=${FILE_ID}" -O kernel-source/stock_boot/boot.img
          else
            curl -L -o kernel-source/stock_boot/boot.img "$BOOT_URL"
          fi
          if file kernel-source/stock_boot/boot.img | grep -q "HTML"; then
            rm kernel-source/stock_boot/boot.img
            exit 1
          fi

      - name: Use boot.img from repository
        if: steps.set_boot_url.outputs.effective_boot_url == ''
        run: |
          if [ -f "boot-stock/boot.img" ]; then
            mkdir -p kernel-source/stock_boot
            cp boot-stock/boot.img kernel-source/stock_boot/
          fi

      - name: Copy build script and patches
        run: |
          cp build_a35.sh kernel-source/
          cp custom.config kernel-source/ 2>/dev/null || true
          cp -r patches kernel-source/ 2>/dev/null || true
          chmod +x kernel-source/build_a35.sh

      - name: Build Kernel
        run: |
          cd kernel-source
          ./build_a35.sh

      - name: Upload Kernel Image
        uses: actions/upload-artifact@v4
        with:
          name: kernel-image
          path: kernel-source/build/Image

      - name: Upload Boot Image
        uses: actions/upload-artifact@v4
        with:
          name: boot-image
          path: kernel-source/build/boot.img

      - name: Create Odin tar
        if: hashFiles('kernel-source/build/boot.img') != ''
        run: |
          cd kernel-source/build
          tar -cvf Custom-Kernel.tar boot.img

      - name: Upload Odin tar
        if: hashFiles('kernel-source/build/Custom-Kernel.tar') != ''
        uses: actions/upload-artifact@v4
        with:
          name: odin-tar
          path: kernel-source/build/Custom-Kernel.tar
